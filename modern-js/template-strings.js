const greetings = "Hello World";
const answer = 'Forty two';

/* evaluate within string and can be in multiple lines */
const html = `<div>
                ${Math.random()}
            </div>`;

console.log(html);